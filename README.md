# WenQu 启停脚本

首次使用前，请在支持 Bash 的终端中为脚本赋予可执行权限：

```bash
chmod +x start.sh stop.sh logs.sh
```

启动服务：

```bash
./start.sh
```

停止服务：

```bash
./stop.sh
```

查看日志：

```bash
./logs.sh
./logs.sh app
./logs.sh milvus
```
